{
  "name": "splitstable-backend",
  "version": "1.0.0",
  "description": "Backend for SplitStable - a group expense splitter that settles in USDC on Arc testnet",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
